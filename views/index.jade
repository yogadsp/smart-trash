extends layout

block content
  h1= title
  
  form(action="/post" method="POST" enctype="multipart/form-data")
    p Input trash image
    input(type="file" name="file")
    input(type="submit" value="Upload File")
  
  br
  p MQTT connected : #{infor}